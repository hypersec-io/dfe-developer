---
- name: Test Ghostty Installation on macOS
  hosts: all
  become: false
  gather_facts: true

  tasks:
    - name: Include ghostty tasks
      ansible.builtin.include_tasks:
        file: roles/dfe_developer/tasks/ghostty.yml
